apiVersion: apps/v1
kind: Deployment
metadata:
  name: teleport-app-vmalertmanager-app-cybozu-com-mysql
  namespace: teleport
spec:
  template:
    spec:
      containers:
      - name: teleport-app
        args:
        - --insecure
        - --roles=app
        - --app-name=vmalertmanager-app-cybozu-com-mysql
        - --app-uri=http://vmalertmanager-vmalertmanager.app-cybozu-com-mysql.svc:9093/
        - --labels=team=dbre
        - --diag-addr=0.0.0.0:3020
