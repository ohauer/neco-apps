apiVersion: cilium.io/v2
kind: CiliumNetworkPolicy
metadata:
  name: admission-test
  namespace: default
spec:
  endpointSelector:
    matchLabels:
      app.kubernetes.io/name: hoge
  ingress:
    - toPorts:
      - ports:
        - port: "8000"
          protocol: TCP
